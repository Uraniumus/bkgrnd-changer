{
  "name": "bcgrnd-changer",
  "version": "1.0.0",
  "description": "App for replace img's background.",
  "main": "index.js",
  "scripts": {
    "start": "node src/server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "dev": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.26.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "2.3.0"
  },
  "lint-staged": {
    "*.js": "eslint --fix",
    "*.{js,css,md}": "prettier --write"
  },
  "dependencies": {
    "backrem": "^1.1.0",
    "express": "^4.17.1",
    "image-size": "^1.0.0",
    "multer": "^1.4.3",
    "uuid": "^8.3.2"
  }
}
